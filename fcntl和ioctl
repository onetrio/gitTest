fcntl:可以获取,改变已打开文件的性质。
	文件描述符不同的位代表了文件不同的性质，通过位操作改变或者得到不同的位的值可以改变和获取文件的各种性质。
	比较重要的操作为O_SYNC，通过设置O_SYNC，设置文件等待写完成，即等待数据写入内存后返回，数据库操作常用。

ioctl：完成其他函数无法完成的I/O操作，例如：磁带操作使我们可以在磁带上写一个文件结束标志,反绕磁带,越过指定个数的文件或记录等等,用本章中的其他函数 (read、write、lseek等)都难于表示这些操作,所以,用 ioctl是对这些设备进行操作的最容易的方法。

